# Authoring notes, not participant-facing
context.md

# macOS noise
.DS_Store

# record-demo artifacts (Playwright install + temp recording dir)
node_modules/
# package-lock.json is committed on purpose: CI needs the exact versions we tested.
.video_tmp/

# Generated locally by make review, never committed
review.md
pr.diff
